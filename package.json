{
  "name": "todos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron": "electron .",
    "build": "electron-packager ./ Todos --arch ia32 --platform win32 --out ./dist/ --version 0.30.2 --overwrite --icon ./notepad.ico"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.7.9",
    "firebase": "^4.6.1"
  },
  "devDependencies": {
    "electron-packager": "^9.1.0"
  }
}
